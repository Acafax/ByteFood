global:
  scrape_interval: 10s

scrape_configs:
  - job_name: 'spring-boot-backend'
    metrics_path: '/actuator/prometheus'
    dns_sd_configs:
      - names:
          - 'backend'
        type: 'A'
        port: 8080